// jsonwebtoken functions such as verify() or sign() use algorithm that needs a secret key (as String) to encode and decode token.
// it can be what ever you want it to be
module.exports = {
  secret: 'uche-secret-key'
};